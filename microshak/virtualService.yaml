apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: microshak
spec:
  hosts:
  - "*"
  gateways:
  - microshak-gateway
  http:
  - route:
    - destination:
        host: microshak
        port:
          number: 80