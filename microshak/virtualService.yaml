apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: microshak
spec:
  hosts:
  - "*"
  gateways:
  - microshak-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        port:
          number: 80
        host: microshak